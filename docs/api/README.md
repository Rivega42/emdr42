# API

Контракты HTTP / WebSocket / RPC.

## Содержание

- `rest-openapi.yaml` — генерируется из NestJS Swagger (`@nestjs/swagger`).
- `websocket-events.md` — все события `socket.io` оркестратора с JSON-схемами payload.
- `webhooks.md` — внешние входящие webhooks (Stripe, Sentry, …).
