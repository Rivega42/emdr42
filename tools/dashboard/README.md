# Tools / Dashboard

React + Vite + Tailwind дашборд состояния задач из GitHub Project v2.

## Источник данных

`scripts/dashboard-fetch.mjs` собирает снимок и кладёт в `tools/dashboard/data/snapshot.json`. Дашборд читает только этот файл.

## Деплой

GitHub Pages из ветки `gh-pages` (Vika-issue из ШАГА 9.4).

## Структура

Заполняется на ШАГЕ 9.
